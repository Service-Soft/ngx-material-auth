import { Environment } from './environment.model';

/**
 * The producrtion environment variables.
 */
export const environment: Environment = {
    production: true,
    apiUrl: 'http://localhost:3000'
};